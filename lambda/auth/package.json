{
  "name": "wordslide-auth-lambda",
  "version": "1.0.0",
  "description": "WordSlide Authentication Lambda Functions",
  "main": "index.js",
  "dependencies": {
    "pg": "^8.11.3",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "scripts": {
    "build": "npm install --production && zip -r ../auth.zip ."
  }
}
