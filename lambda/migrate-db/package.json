{
  "name": "wordslide-migrate-db",
  "version": "1.0.0",
  "description": "WordSlide Database Migration Lambda Function",
  "main": "index.js",
  "dependencies": {
    "pg": "^8.11.3"
  },
  "scripts": {
    "build": "npm install --production && zip -r ../migrate-db.zip ."
  }
}
